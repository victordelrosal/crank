# Handoff 2026-07-27 22:16 (crank, runs 4 and 4b)

## Resume here

Nothing is outstanding in this repo. If you want the next real move, it is the shard-placement
debt: `~/.claude/crank/learnings/loop.md` holds a handful of rules that belong in `web-render.md`
(a bloom effect, a thunder mix, a `::after` cascade trap), left there by the keyword migration.
It is self-correcting and low value. Genuinely optional.

## State now

- **Run 4 shipped** (`89397d7`): Victor imported "The Knowledge Graph Playbook for Agentic
  Systems" and asked whether crank needs changes. Answer, argued: mostly no. Of ~30 mechanisms,
  16 were already in the skill under other names; 4 genuine gaps adopted; a knowledge graph
  refused with the argument written down. 12 of 12 criteria PASS on a cold verifier.
- **Lars killed the source's authority.** Web-verified that the playbook misstates three Anthropic
  sources it leans on: a foundational quotation not locatable in any Anthropic publication, two
  per-document precision rows read as a corpus score, and `parallelization` silently dropped from
  the five agent workflows. The layered stack both July 2026 usages inherit is third-party, not
  Anthropic's. Nothing entered the skill on the document's word.
- **The "graph" naming collision is pinned.** Run 3 defined graph = topology at 01:40; this
  document claimed the same word for shared memory at 20:57 the same day. `skill/SKILL.md` and
  `README.md` now both say: in crank, "graph" is always topology, and the memory sense is always
  written "knowledge graph" in full.
- **The global craft memory was broken and is now fixed.** It had reached 738 lines / 69,859 bytes
  / 179 rules, larger than the skill itself, against its own rule that it stay readable in under a
  minute. Restructured into an index (~5.4KB) plus 7 capped topic shards, a verbatim ARCHIVE, and
  a new gate. All 179 rules relocated, counted in and out.
- **Run 4b shipped** (`5f89ee2`): built `~/.claude/crank/tools/shoot.sh` and retired 7 screenshot
  rules into it. First time this memory has shrunk per unit of capability.
- **The tool is verified by driving it**, not reading it: 390px viewport honored exactly (the
  documented 500px Rosetta floor defeated, confirmed from the rendered PNG rather than the tool's
  self-report), background image painted on the kept second shot, WebGL alive under swiftshader,
  full-page tall capture in 2.9s with no stall, zero leaked processes across normal exit, error
  exits, SIGINT and SIGKILL.
- **A cold verifier failed the tool on exit codes** and was right: three wrong-image conditions
  returned 0. Now exit 4 SUSPECT (non-2xx main document, viewport not honored, network never
  idle), `--lenient` to override. 11 of 11 exit codes re-verified.
- **The memory gate now actually blocks.** `learnings-check.sh` is wired into
  `.loop/tools/check.sh`, proved by negative test to fail the run, with the cross-machine
  dependency documented and a missing tool treated as FAIL, not WARN.
- **Memory accounting**: 179 baseline, minus 7 retired, plus 4 added, equals 176 live.
- Working tree clean, `origin/master` in sync, both gates green, zero processes running.

## What's left

1. Nothing required in crank.
2. Optional: pay the shard-placement debt described above.
3. Optional: `python3 -m playwright install webkit` if you ever need the iOS engine. `shoot.sh
   --webkit` currently exits 3 with that exact command in the message.
4. Outside crank, waiting on Victor (not touched this session): 6 unfired gates in
   `sBs/AE/PENDING-GATES.md` (oldest 111 days) and MSc supervision (Lili Fan awaiting a reply,
   one draft staged).

## Files touched

- `skill/SKILL.md` (+55 net) and `~/.claude/skills/crank/SKILL.md` (kept byte-identical)
- `research/knowledge-graph-playbook-anthropic-2026-07.pdf` (imported from Downloads)
- `research/knowledge-graph-playbook-anthropic-2026-07.md` (new capture, covers/lacks + sourcing)
- `README.md` (the "One word, two disciplines" paragraph)
- `LEARNINGS.md` (workspace: 2 new rules)
- `.loop/BRIEF.md`, `.loop/CRITERIA.md`, `.loop/LOG.md`, `.loop/run3/` (run 3 archived)
- `.loop/tools/check.sh` (6 new rule greps, memory gate wired in)
- `~/.claude/crank/LEARNINGS.md` (rewritten as an index)
- `~/.claude/crank/learnings/{loop,verify,web-render,shell-tooling,deploy-infra,authoring,data-llm}.md`
- `~/.claude/crank/learnings/ARCHIVE.md`, `ARCHIVE-2026-07-27-pre-shard.md` (verbatim backup)
- `~/.claude/crank/tools/shoot.sh` (new), `~/.claude/crank/tools/learnings-check.sh` (new)

## Pointers

- Commits: `89397d7` (run 4), `5f89ee2` (run 4b), `00065a7` (log close). All pushed to
  `github.com/victordelrosal/crank`.
- The argument for refusing a knowledge graph, with the premise to attack, is in
  `research/knowledge-graph-playbook-anthropic-2026-07.md` under "The verdict on the memory layer".
- Run gates: `bash .loop/tools/check.sh` (includes the memory gate).
- Tool contract: `~/.claude/crank/tools/shoot.sh -h`.
- Honest limit recorded in the tool header: content scheduled by a bare `setTimeout` with no
  network request behind it is invisible to every load signal; `--wait` is the answer.
- `WHISPER.md` is gitignored and must never reach the public remote. Never `git add -A` here.
